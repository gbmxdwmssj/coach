import rospy
import numpy as np
from w_ori_entity import WOriEntityEnv

rospy.init_node('test_worien_rviz', anonymous=True)

env = WOriEntityEnv()

while not rospy.core.is_shutdown():
    _, _, d, _ = env.step(np.array([5.0]))
    if d:
        env.reset()

print('Finished!')
