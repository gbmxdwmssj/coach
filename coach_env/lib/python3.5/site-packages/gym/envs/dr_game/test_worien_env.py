import rospy
import numpy as np
from worien import WorienEnv

env = WorienEnv()

r = rospy.Rate(10)
while not rospy.core.is_shutdown():
    r.sleep()
    _, _, d, _ = env.step(np.array([2.0]))
    if d:
        env.reset()

print('Finished!')
